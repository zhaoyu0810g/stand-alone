import React, { ReactNode, memo } from 'react';

export const ConnectorPage: React.FC = memo((): ReactNode => {
    return (
        <div>
            ConnectorPage
        </div>
    )
})

ConnectorPage.displayName = "ConnectorPage";
